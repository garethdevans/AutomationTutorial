require 'rubygems'
require 'cucumber'
require 'cucumber/rake/task'

Cucumber::Rake::Task.new(:default) do |t|
  t.profile = :default
end

Cucumber::Rake::Task.new(:prod) do |t|
  t.profile = :prod
end
